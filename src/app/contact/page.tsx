import { ContactPanel } from '@/components/panels/contact-panel'

export const metadata = {
  title: 'Contact',
  description: 'Contribute to Kranix IO — docs, GitHub, discussions, and CLI install.',
}

export default function ContactPage() {
  return <ContactPanel />
}
