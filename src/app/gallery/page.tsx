import { GalleryPanel } from '@/components/panels/gallery-panel'

export const metadata = {
  title: 'Gallery',
  description: 'Kranix architecture diagrams, CLI screenshots, and platform visuals.',
}

export default function GalleryPage() {
  return <GalleryPanel />
}
